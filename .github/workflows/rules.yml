name: Rules check

on:
  pull_request:
    paths:
      - '**/*' 

jobs:
  check-newline:
    runs-on: self-hosted
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Check for newline at end of files
        run: python3 .github/workflows/scripts/newline.py
